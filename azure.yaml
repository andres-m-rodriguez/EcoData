# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: eco-data
services:  
  app:
    language: dotnet
    project: ./src/Host/EcoData.AppHost/EcoData.AppHost.csproj
    host: containerapp
