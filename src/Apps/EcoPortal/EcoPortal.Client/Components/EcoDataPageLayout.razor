@using EcoPortal.Client.Services
@inject INavigationService Navigation

<div class="page-container mt-4 mb-8">
    @if (!string.IsNullOrEmpty(ParentPath))
    {
        <MudHidden Breakpoint="Breakpoint.Xs">
            <MudButton Variant="Variant.Text"
                       StartIcon="@Icons.Material.Filled.ArrowBack"
                       OnClick="GoBack"
                       Class="mb-2">
                Back
            </MudButton>
        </MudHidden>
    }

    @if (!string.IsNullOrEmpty(Label) || Actions is not null)
    {
        <div class="page-header mb-4">
            <MudStack Row Justify="Justify.SpaceBetween" AlignItems="AlignItems.Center">
                <MudStack Spacing="1">
                    @if (!string.IsNullOrEmpty(Label))
                    {
                        <MudText Typo="Typo.overline" Color="Color.Secondary">
                            @Label
                        </MudText>
                    }
                </MudStack>

                @if (Actions is not null)
                {
                    <MudHidden Breakpoint="Breakpoint.Xs">
                        <div class="page-actions">
                            <MudStack Row Spacing="2">
                                @Actions
                            </MudStack>
                        </div>
                    </MudHidden>
                }
            </MudStack>
        </div>
    }

    <div class="page-content">
        @ChildContent
    </div>
</div>

@code {
    [Parameter]
    public string? Label { get; set; }

    [Parameter]
    public string? Title { get; set; }

    [Parameter]
    public string? ParentPath { get; set; }

    [Parameter]
    public RenderFragment? Actions { get; set; }

    [Parameter]
    public string? ActionTitle { get; set; }

    [Parameter]
    public Action? OnActionClick { get; set; }

    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    protected override void OnInitialized()
    {
        // Set title immediately so it appears during page transition
        Navigation.SetPageTitle(Title);

        if (ParentPath is not null)
        {
            Navigation.SetParentPath(ParentPath);
        }

        // Always set or clear the action to ensure proper state
        if (ActionTitle is not null && OnActionClick is not null)
        {
            Navigation.SetPageAction(ActionTitle, OnActionClick);
        }
        else
        {
            Navigation.ClearPageAction();
        }
    }

    private void GoBack() => Navigation.GoBack();
}
