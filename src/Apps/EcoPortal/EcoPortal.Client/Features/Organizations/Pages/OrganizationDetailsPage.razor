@page "/organizations/{Id:guid}"
@using EcoPortal.Client.Features.Organizations.Workspace

@* Overview for members, the public profile for everyone else — the workspace
   decides which from the loaded context. *@

<OrganizationWorkspace OrganizationId="Id" Section="OrganizationSection.Overview">
    <OrganizationOverview Context="context" />
</OrganizationWorkspace>

@code {
    [Parameter]
    public Guid Id { get; set; }
}
