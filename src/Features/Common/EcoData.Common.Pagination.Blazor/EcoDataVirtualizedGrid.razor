@using Microsoft.AspNetCore.Components.Web.Virtualization
@typeparam TItem
@typeparam TParams where TParams : CursorParameters

@if (_isInitialLoading)
{
    @if (LoadingTemplate is not null)
    {
        @LoadingTemplate
    }
}
else if (_isEmpty)
{
    @if (EmptyTemplate is not null)
    {
        @EmptyTemplate
    }
}
else
{
    <div class="@ComputedClass">
        <Virtualize TItem="IReadOnlyList<TItem>"
                    @ref="_virtualizeRef"
                    @key="_generation"
                    ItemsProvider="LoadRowsAsync"
                    ItemSize="ItemSize"
                    OverscanCount="OverscanCount">
            <ItemContent Context="row">
                <div class="ecodata-virtualized-grid-row" style="@RowVariables">
                    @foreach (var item in row)
                    {
                        @ItemTemplate(item)
                    }
                </div>
            </ItemContent>
            <Placeholder>
                <div class="ecodata-virtualized-grid-row" style="@RowVariables">
                    @for (var i = 0; i < EffectiveColumns; i++)
                    {
                        @if (PlaceholderTemplate is not null)
                        {
                            @PlaceholderTemplate
                        }
                    }
                </div>
            </Placeholder>
        </Virtualize>
    </div>
}
