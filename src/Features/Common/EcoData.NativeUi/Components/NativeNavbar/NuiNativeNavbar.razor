@namespace EcoData.NativeUi.Components.NativeNavbar
@inherits StatefulComponent
@inject INavbarManager Navbar
@inject INavigationManager Navigation

@* Back button *@
@if (ShowBackButton && Navigation.State.CanGoBack)
{
    <MudIconButton Icon="@Icons.Material.Filled.ArrowBack"
                   Color="Color.Dark"
                   OnClick="GoBackAsync" />
}

@* Title *@
@if (ShowTitle && Navbar.State.Title is not null)
{
    <span class="nui-navbar-title @TitleClass">
        @Navbar.State.Title
    </span>
}

@* Actions *@
@if (ShowActions && Navbar.State.Actions.Count > 0)
{
    @if (Navbar.State.Actions.Count == 1)
    {
        <MudButton Variant="Variant.Filled"
                   Color="Color.Primary"
                   Size="Size.Small"
                   OnClick="@(() => Navbar.State.Actions[0].OnClick())">
            @Navbar.State.Actions[0].Title
        </MudButton>
    }
    else
    {
        <MudMenu Icon="@Icons.Material.Filled.MoreVert"
                 Color="Color.Dark"
                 AnchorOrigin="Origin.BottomRight"
                 TransformOrigin="Origin.TopRight">
            @foreach (var action in Navbar.State.Actions)
            {
                <MudMenuItem OnClick="@(() => action.OnClick())">
                    @action.Title
                </MudMenuItem>
            }
        </MudMenu>
    }
}

@code {
    /// <summary>
    /// Whether to show the back button when navigation allows going back.
    /// </summary>
    [Parameter]
    public bool ShowBackButton { get; set; } = true;

    /// <summary>
    /// Whether to show the page title.
    /// </summary>
    [Parameter]
    public bool ShowTitle { get; set; } = true;

    /// <summary>
    /// Whether to show action buttons.
    /// </summary>
    [Parameter]
    public bool ShowActions { get; set; } = true;

    /// <summary>
    /// CSS class for the title element.
    /// </summary>
    [Parameter]
    public string? TitleClass { get; set; }

    // The bar renders straight off the two managers' State, so the handlers have
    // no work of their own: the subscription itself is the re-render.
    [Event]
    private void OnNavbarChanged(NavbarChanged _) { }

    [Event]
    private void OnNavigationChanged(NavigationChanged _) { }

    private async Task GoBackAsync()
    {
        await Navigation.GoBackAsync();
    }
}
